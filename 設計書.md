# マンナカ 設計書

N人会議場所最適化エージェント（Google Cloud Japan AI Hackathon vol.5）

この文書は**実装されているもの**を書く。構想段階で検討して落とした機能は §3 に
「持たないもの」として理由つきで残す。README.md は使い方、CONTRIBUTING.md は
開発手順、DEPLOY.md はデプロイ手順を扱う。

## 1. 概要

参加者全員の出発駅から、宣言した公平性ポリシーで集合場所の候補を並べ、
**誰がどれだけ負担しているか**を人ごとに可視化する。

- **一言価値**: 「いつも本社」で終わる場所決めを、根拠を言える比較に置き換える
- **設計の核**: どこに集まるかは決めない。候補と、その根拠と、負担の偏りを出すまでが仕事

## 2. 課題

| 課題 | 現状 |
|---|---|
| 場所決めの恣意性 | 惰性で本社。遠い側が毎回、時間も運賃も余分に払う |
| 人数が増えると決められない | 中間地点の検索は2人前提のものが多く、N人の負担配分を扱わない |
| 誰が損したか見えない | 決まった場所だけが共有され、内訳は誰も見ない |

## 3. スコープ

### やること

- 名前と出発駅の入力（ログインなし）
- 出発駅集合から候補となる結節駅を列挙し、N人分の経路を並列取得
- 3つの公平性ポリシーでの評価と、切り替えによる結論の変化の提示
- 1人ごとの所要・運賃・乗換の内訳と、地図・表情による負担の可視化
- どの基準でどう評価したかの監査ログ

### 持たないもの

いずれも構想段階では入れていたが、「場所を比べる」一点に絞るために落とした。

| 落としたもの | 理由 |
|---|---|
| Slack / Google Chat 連携 | 入口を増やしても比較の質は上がらない。Webフォームで足りる |
| 会議室の空き照会・予約 | 実APIが無くモックにしかならず、モックの予約に価値がない |
| 支出上限・承認ゲート | 予約を持たないので、承認する支出が存在しない |
| カレンダー登録 | 決定を握るのは人。決まっていないものを登録できない |
| 当日の遅延フォロー | 当日の運行実況を継続監視する常駐処理が必要で、規模が合わない |
| ログイン・通知 | 個人情報を増やさない方針（§8）と直接ぶつかる |

## 4. アーキテクチャ

![技術スタック](docs/mannaka_architecture.png)

```
ブラウザ（web/ を Cloud Run が静的配信）
      │  POST /api/meetings
      ▼
FastAPI（app/api/routes.py）
      │
      ▼
OrchestratorAgent ── 依頼の受付・進行管理
      └── OptimizerAgent ── 候補列挙・N人分経路の並列取得・ポリシー評価
            │
            ├── TransitPort ── mock（既定）/ ekispert（駅すぱあとAPI）
            └── LlmPort ────── stub（既定）/ gemini
      │
      ├── Repository ──── firestore（既定）/ memory
      └── AuditService ── Firestore ＋ 構造化JSONログ（Cloud Logging）
```

単一の Cloud Run サービスで API と画面の両方を配信する。API とエージェントを
分けていないのは、この規模で分ける利点が無いため。

### Ports & Adapters

ドメイン層とエージェントは `app/ports/` のインターフェースしか見ない。
モックと実APIの両方を知っているのは `app/adapters/registry.py` **だけ**で、
実API側の初期化に失敗したらモックに落として起動を継続する。
どちらで動いているかは `/health` に出る。

| ポート | 責務 | 実装 |
|---|---|---|
| `TransitPort` | 結節駅の列挙、1人分の経路（所要・運賃・乗換・出発時刻）、駅座標 | `mock` / `ekispert` |
| `LlmPort` | 希望条件の解釈、候補比較の説明文 | `stub` / `gemini` |
| `Repository` | 会議と監査ログの永続化 | `firestore` / `memory` |

`TransitPort.known_stations()` は扱える駅名の集合を返す（列挙できない実装は
`None`）。未知の駅を黙って近くの駅に寄せると、その人だけ0分0円で来られること
になり、どの候補が最良かという結論そのものが狂う。だから受け付ける前に弾く。

`TransitPort.coordinates()` は駅の実座標（緯度・経度）を返すが、**地図を描くため
だけ**に使う。評価には一切入らない。

## 5. 最適化ロジック

`app/domain/optimization.py`。外部依存のない純粋関数群で、経路の取得は
ポート側の責務として切り離してある。

1. **候補列挙** — 出発駅集合から到達性の高い結節駅を `candidate_limit` 件（既定8）
2. **経路取得** — 候補ごとに全員分を `asyncio.gather` で並列取得。到着時刻は
   集合時刻から `buffer_minutes`（既定10分）を引いた着時刻で引く
3. **評価** — ポリシー別のスコア（小さいほど良い）で並べ、`rank` を振る

| ポリシー | 画面の表示 | スコア |
|---|---|---|
| `sum` | 早い | 合計移動時間（分） |
| `minimax` | バランスよく | 最大所要 × 1000 ＋ 合計時間（同点は合計時間で解く） |
| `cost` | 安い | 総運賃 × 1000 ＋ 合計時間（同点は合計時間で解く） |

補助指標として `unfairness`（最長と最短の所要時間差）を各候補に添える。
既定は `minimax`。希望条件の文から読み取れた場合はそれを優先する。

**同じ参加者でも、宣言する基準で結論が変わる。** これを隠さず表に出すことが
このプロダクトの主張そのもので、`GET /api/meetings/{id}/policy-comparison` は
3基準の結論の差だけを返す。

## 6. エージェント構成

| エージェント | 役割 | 自律性 |
|---|---|---|
| `OrchestratorAgent` | 依頼の受付、入力の検証、進行管理、監査記録 | 対話駆動 |
| `OptimizerAgent` | 候補列挙・経路取得・ポリシー評価・基準間の比較 | 算出と比較は自律 |

ADK（Agent Development Kit）では `app/agents/adk_agent.py` が Orchestrator の
操作を FunctionTool として `LlmAgent` に公開し、自然文から候補算出を駆動できる
ようにしている。**場所を選ぶツールは公開しない。** 決めるのは人だという設計を、
ツールの有無として強制する。ADK / Gemini が使えない環境では
`build_root_agent()` が `None` を返し、REST API 側はそのまま動く。

## 7. API・データモデル

### REST API

| メソッド | パス | 内容 |
|---|---|---|
| POST | `/api/meetings` | 参加者と日時から候補地を算出 |
| GET | `/api/meetings/{id}` | 会議の取得 |
| POST | `/api/meetings/{id}/optimize` | 基準を入れ替えて算出し直す |
| GET | `/api/meetings/{id}/policy-comparison` | 3基準の結論の差 |
| GET | `/api/meetings/{id}/audit` | その会議の監査ログ |
| GET | `/health` | 稼働確認とプロバイダの表示 |

`/healthz` は使わない。Cloud Run の手前の Google フロントエンドがこのパスを
横取りし、コンテナに届く前に404を返す（実測で確認済み。`app/main.py` に理由を
コメントで残してある）。

### Firestore

```
meetings/{meetingId}
  status: draft | optimized
  request: { starts_at, participants[{uid, display_name, origin_station}], raw_text }
  policy: sum | minimax | cost        # 採用した公平性基準
  optimization: {
    policy, evaluated_at, explanation,
    candidates: [{ area, station, legs[], total_minutes, max_minutes,
                   total_fare_yen, total_transfers, score, rank }],
    places: { 駅名: [緯度, 経度] }     # 描画用
  }
audit/{logId}
  { meeting_id, actor, action, payload, created_at }
```

`uid` はリクエスト内の連番（`p1`, `p2`, …）で、アカウントではない。
住所も連絡先も持たない。

## 8. ガバナンス・セキュリティ

| 方針 | 実装 |
|---|---|
| 個人情報を増やさない | 受け取るのは名前と出発駅だけ。ログインも通知も持たない |
| 判断は人が握る | 場所を決めない。ADK にも場所を選ぶツールを公開しない |
| 公平性の宣言 | `policy` は会議に必ず保存され、説明文にも監査ログにも出る |
| 可観測性 | `AuditService` が Firestore と構造化JSONログの両方へ記録。候補の内訳も payload に含む |
| 秘密情報 | `.env`（gitignore対象）から注入。本番は Secret Manager 経由で Cloud Run へ。イメージには焼き込まない |

## 9. 技術スタック・設定

| 区分 | 技術 |
|---|---|
| 実行基盤 | Cloud Run（FastAPI / `python:3.12-slim`） |
| AI | Gemini API、Agent Development Kit（ADK） |
| スポンサー | 駅すぱあと API（`/search/course/extreme` ほか） |
| データ | Firestore（ローカルはエミュレータ） |
| 可観測性 | Cloud Logging（構造化JSONの監査ログ） |
| 画面 | 素の HTML / CSS / JS。地図は MapLibre GL JS ＋ CARTO Positron（鍵なし） |

差し替えはすべて環境変数。設定項目の一覧は `.env.example` にあり、
`app/settings.py` が読まない変数はそこにも置かない。

| 変数 | 既定 | 実APIにするとき |
|---|---|---|
| `TRANSIT_PROVIDER` | `mock` | `ekispert` ＋ `EKISPERT_API_KEY` |
| `LLM_PROVIDER` | `stub` | `gemini` ＋ `GOOGLE_API_KEY` |
| `REPOSITORY` | `firestore` | — （`memory` はテスト用の揮発ストア） |

## 10. 開発・デプロイ

ローカルから本番まですべてコンテナに閉じる。ホストに Python も graphviz も
入れない。ローカル・CI・本番が同じ Dockerfile を参照する。

```
Dockerfile           # 本体（Cloud Run と同一イメージ）
Dockerfile.docs      # アーキテクチャ図の生成（graphviz）
Dockerfile.shots     # 紹介ページ用の画面キャプチャ（Chromium）
compose.yaml         # api / firestore エミュレータ ＋ docs / shots / test プロファイル
```

デプロイは `gcloud run deploy --source .` で Cloud Build 経由。GitHub Actions の
CD は Workload Identity Federation で認証し、`CD_ENABLED` 変数が `true` の
ときだけデプロイまで進む。詳細は DEPLOY.md。

## 11. 現状と残り

**実装済み**: 入力フォーム、候補算出と3ポリシー、負担の可視化（地図・表情）、
希望条件からの基準の読み取り、監査ログ、REST API、ADKエージェント定義、
Firestoreリポジトリ、Cloud Run へのデプロイ。

駅すぱあとAPI・Gemini とも実キーで疎通を確認済み。

**残り**:

- **Gemini が `503`（高負荷）を返すと候補算出ごと `500` で落ちる。** LLM は
  希望条件の解釈と説明文の生成にしか使っておらず、失敗してもモック同様に
  続行できるはずの箇所。`LlmPort` の呼び出し側にフォールバックを足す
- 本番 Firestore の準備（`firestore.googleapis.com` の有効化、Native モードでの
  データベース作成、Cloud Run のサービスアカウントへ `roles/datastore.user`）
- 駅すぱあとは REST を直接叩いており、MCPサーバー経由にはしていない
