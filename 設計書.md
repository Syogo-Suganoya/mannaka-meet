# マンナカ 設計書

N人会議場所最適化エージェント（CANDIDATE 06）

## 1. 概要

参加者全員の出発地から、合計移動コスト最小（または最大負担平準化）の会議場所を選定し、会議室の確保・個別経路の配信・当日の遅延フォローまで一括執行するエージェント。

- **一言価値**: 「いつも本社」で終わる場所決めを、公平性ポリシー付きの最適化とワンストップ手配に置き換える
- **テーマ適合**: 「どの公平性基準で最適化したか」を宣言・記録する透明な意思決定と、金銭支出の権限制御

## 2. 課題

| 課題 | 現状 |
|---|---|
| 場所決めの恣意性 | 多拠点・ハイブリッド時代でも対面会議は惰性で本社。遠い側が毎回負担 |
| 中間地点ツールの限界 | 既存は2人用の待ち合わせ遊び。N人×会議室空き×交通実況の統合はない（検索確認済み） |
| 決定後の手配が分断 | 会議室予約・招集・経路案内・精算が全部別作業 |

## 3. ユースケース

1. **社内多拠点会議**: 6人・3拠点の定例を毎回最適地で
2. **社外パートナーとの打合せ**: 双方の移動負担を「見える公平さ」で調整
3. **拡張**: 勉強会・オフ会・面接（候補者の負担配慮）・貸会議室事業者の送客

## 4. システムアーキテクチャ

```
[Slack / Google Chat（「来週火曜14時、この6人で」）]
      │
      ▼
[API Gateway (Cloud Run: api)]
      │
      ▼
[Orchestrator Agent (ADK / Cloud Run: agent)]
  ├── 最適化エージェント ────── 駅すぱあと API MCPサーバー（N人分経路・運賃）
  ├── 手配エージェント ──────── 会議室API（スペイシー等モック）/ Google Calendar API
  ├── 配信エージェント ──────── 個別経路・出発時刻・運賃の一斉配信
  └── 当日フォローエージェント ─ 遅延監視→開始時刻調整の起案
      │
      ▼
[Firestore]（会議・参加者プロフィール）
[Cloud Logging]（監査ログ：採用した公平性ポリシー・支出承認）
```

### エージェント構成（ADK）

| エージェント | 役割 | 自律性 |
|---|---|---|
| Orchestrator | チャット依頼の解釈、進行管理 | 対話駆動 |
| 最適化 | 候補エリア算出（総移動時間/運賃/乗換負荷）、公平性ポリシー適用 | 算出・比較は自律 |
| 手配 | 会議室の空き照会・仮押さえ、カレンダー登録 | **有料予約は主催者承認＋上限額** |
| 配信 | 確定後の個別経路・精算用運賃の配信 | 自律実行 |
| 当日フォロー | 運行障害の監視、影響判定、開始時刻調整の起案 | 起案まで |

### 最適化ロジック

- 候補エリア: 参加者の出発駅集合から到達性の高い結節駅を列挙（駅すぱあとMCP）
- 評価関数（選択式の公平性ポリシー）:
  - `sum`: 合計移動時間最小
  - `minimax`: 最大負担の平準化（いちばん遠い人を最も楽に）
  - `cost`: 総運賃最小（経費重視）
- 各候補に内訳（1人ごとの所要・運賃）を添えて提示し、選択されたポリシーを監査ログに記録

## 5. 使用API・技術スタック

| 区分 | 技術 |
|---|---|
| 実行基盤（必須要件） | Cloud Run |
| AI（必須要件） | Gemini API（依頼解釈・候補比較の説明文・調整文）、Agent Development Kit |
| スポンサー | 駅すぱあと API MCPサーバー（N人分経路・運賃・当日実況） |
| 連携 | Google Calendar API、Slack / Google Chat、会議室予約API（モック） |
| データ | Firestore |

## 6. データモデル（Firestore）

```
users/{uid}
  profile: { 表示名, 出発駅 }        # 自宅住所は保持しない
meetings/{meetingId}
  request: { 日時, 参加者[], 要件(人数/設備) }
  policy: sum | minimax | cost       # 採用した公平性基準
  candidates: [ { エリア, 合計分, 内訳[] } ]
  decision: { 場所, 会議室, 承認者 }
  routes: { uid: {経路, 出発時刻, 運賃} }
audit/{logId}   # ポリシー選択・支出承認・当日リスケの証跡
```

## 7. ガバナンス&セキュリティ設計

1. **自宅を晒さない**: 登録は最寄り駅のみ。他参加者には候補地と所要時間だけが見え、誰がどこから来るかは非開示
2. **支出の権限制御**: 有料会議室は主催者承認制＋エージェントの支出上限額を設定。承認履歴を監査ログへ
3. **公平性の宣言**: どの評価関数で選んだかを常に明示・記録し、場所決めの恣意性を排除
4. **可観測性**: 候補算出の内訳・当日リスケの判断根拠をすべて構造化ログに残す

## 8. 環境構築（Docker）

ローカル開発から本番まで、すべてコンテナで統一する（Cloud Run はコンテナデプロイのため、ローカルと本番で同一イメージを使用できる）。

### 構成

```
.
├── docker-compose.yml        # ローカル開発一式
├── services/
│   ├── api/Dockerfile        # API Gateway（FastAPI）
│   ├── agent/Dockerfile      # ADKエージェント本体
│   └── web/Dockerfile        # フロント（PWA、開発時のみ）
└── .env.example              # APIキーのテンプレート（実キーはコミットしない）
```

### docker-compose サービス

| サービス | 内容 |
|---|---|
| api | API Gateway。ホットリロード有効（ソースをボリュームマウント） |
| agent | ADKエージェント。Gemini API・スポンサーAPIへの接続を担う |
| web | フロント開発サーバー |
| firestore-emulator | Firestoreエミュレータ（gcloud CLIイメージ）。本番データに触れず開発 |

- `docker compose up` で全サービス起動。APIキー類は `.env`（gitignore対象）から注入
- 本番の秘密情報は Secret Manager で管理し、Cloud Run に環境変数として注入（イメージに焼き込まない）

### イメージとデプロイ

- ベースイメージ: `python:3.12-slim`（マルチステージビルドで依存を分離し軽量化）
- デプロイ: `gcloud builds submit` → Cloud Build でビルド → Artifact Registry へ push → Cloud Run へリリース
- ローカル・CI・本番が同一 Dockerfile を参照し、環境差異による不具合を構造的に排除

## 9. MVPスコープ（〜10/15）

| 週 | マイルストーン |
|---|---|
| W1 | 駅すぱあとMCP接続、N人経路の並列取得と評価関数 |
| W2 | 候補提示UI（内訳・ポリシー切替） |
| W3 | 手配（Calendar登録・会議室モック・承認フロー） |
| W4 | 個別配信＋当日遅延フォロー |
| W5 | Slack連携・監査ログ・デプロイ |
| W6 | デモ動画・提出物 |

**MVPで捨てるもの**: 実会議室API連携（モック）、経費精算システム連携

## 10. 審査基準との対応

| 基準 | 対応 |
|---|---|
| 課題の新規性 | N人×会議室×交通実況の統合は空白（検索立証）。公平性ポリシーの概念が独自 |
| 自律性・エージェントらしさ | 依頼→算出→手配→当日フォローの一気通貫＋承認ゲート |
| 実装品質と拡張性 | 駅すぱあとMCPのみで中核成立＝完成度を最も高く仕上げられる。費用対効果の説明が容易 |

## 11. 備考

- 5案中、実装リスクが最も低い「安全牌」。ただしYouCam・GMIを使わないため、スポンサー活用度では他案に劣る
